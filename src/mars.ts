import readFile from './utils/file';

export default function readInstructions(input: string) {
  // read File
  const file = readFile(input);

  console.log(file);
  // load Map
  // load Robots
}

// do logic
